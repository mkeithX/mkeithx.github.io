---
description: The official DevPortal of the Kitiplex DevOps Team.
slug: /
sidebar_label: Introduction
keywords:
  - docs
  - mkeithX
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import IframeWindow from "@site/src/components/BrowserWindow/IframeWindow";
import BrowserWindow from "@site/src/components/BrowserWindow";
import {
  QuickStartCardRow,
  ReportsCardRow,
} from "@site/src/components/QuickStartCards";


# Welcome
**MKX SpaceHub** is a simple documentation website that offers both basic and extended resources for systems administration and web development. It features tutorials that guide users through essential concepts, code snippets that provide quick solutions, and gists for sharing reusable code. The site also includes user guides for various projects, too!


<!-- <QuickStartCardRow /> -->


<Columns className='item margin-bottom-lg' > 

<Column  className='text--left item  padding--sm'>
<Card shadow='tl' style={{border: '1.5px solid var(--ifm-color-primary)'}}>
    <CardHeader>
<div class="avatar">
  <img
    class="avatar__photo avatar__photo--md"
    src="/img/logo/x_code_slash.svg" />
  <div class="avatar__intro">
    <div class="avatar__name"><h3>Guides</h3></div>
    <!-- <small class="avatar__subtitle">Frenchy Front End Engineer</small> -->
  </div>
</div>
    </CardHeader>
    <CardBody>
Advanced tips, guides and best practices for mitigating issues. [Take me there](/docs/devops/guides-and-workarounds).
    </CardBody>
  </Card>
  </Column>
   <Column  className='text--left item  padding--sm'>
<Card shadow='tl' style={{border: '1.5px solid var(--ifm-color-primary)'}}>
    <CardHeader>
<div class="avatar">
  <img
    class="avatar__photo avatar__photo--md"
    src="/img/rocket-1a.svg" />
  <div class="avatar__intro">
    <div class="avatar__name"><h3>The Universe</h3></div>
    <!-- <small class="avatar__subtitle">Frenchy Front End Engineer</small> -->
  </div>
</div>
    </CardHeader>
    <CardBody>
Discover [fascinating facts](/cosmos) about the universe starting from our [Solar System](/cosmos/solar-system).
    </CardBody>
  </Card>
  </Column>
</Columns>

## Motivation
MKX SpaceHub is part of an internal collaboration within the [Kitiplex](https://github.com/kitiplex) organization, designed and maintained by [@mkeith**X**](https://github.com/mkeithx). The project aims to create an intuitive Knowledge Base Portal using powerful, low-maintenance technology, ensuring that content remains both accessible and reliable.


<!-- Designed and maintained by [@mkeithX](https://github.com/mkeithx), the website is built using [Docusaurus](https://docusaurus.io/), a popular static-site generator known for its ease of use and flexibility. The site’s source code is hosted on [GitHub](https://github.com/login), allowing for collaborative development and version control. It is also hosted on [**Cloudflare**](https://developers.cloudflare.com/pages)., ensuring fast and reliable performance with enhanced security features. -->


The website is built using [Docusaurus](https://docusaurus.io/), a popular static site generator known for its ease of use and flexibility. The site’s source code is available on [GitHub](https://github.com/login), and it is also hosted on [Cloudflare](https://developers.cloudflare.com/pages), ensuring fast and reliable performance with enhanced security features.

## Live Editor
Test your React using the Interactive Code Editor:

<BrowserWindow>


```jsx live noInline
const project = 'The SpaceHub Project';

const Greeting = () => <p>Introducing <br></br> {project}</p>;

render(<Greeting />);
```

</BrowserWindow>

### Samples

<details>
  <summary>**Real-Time Clock**</summary>

Displays the current time and updates every second. It uses `useState` to manage the time state and `useEffect` to set up and clean up a timer for real-time updates.

```jsx showLineNumbers
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
render(Clock)
```

</details>


<details>
<summary>**Enhanced Clock**</summary>

Showing both 24-hour and 12-hour formats.

```js showLineNumbers
function Clock(props) {
  const [date, setDate] = useState(new Date());

  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return () => clearInterval(timerID); // Cleanup timer on unmount
  }, []); // Add empty dependency array for one-time setup

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h4>
        Date: {date.toLocaleDateString()}, {date.toLocaleDateString(undefined, { weekday: "long" })}
      </h4>
      <h4>
        Time: {date.toLocaleTimeString([], { hour12: false })} · {date.toLocaleTimeString([])}
      </h4>
    </div>
  );
}

render(<Clock />);
```
</details>


<details>
<summary>**Likes Counter**</summary>

A simple component that allows users to increase a like count. 

```jsx showLineNumbers
const LikeButton = (props: Props) => {
  const [likes, increaseLikes] = React.useState<number>(0);
  
  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
      <h3 style={{
        background: 'darkslateblue',
        color: 'white',
        padding: 8,
        borderRadius: 4,
        textAlign: 'center', // Center text within the h3
      }}>
        {props.label} {likes} Likes
      </h3>
      <button
        className="button button--md button--danger"
        onClick={() => increaseLikes(c => c + 1)}
      >
        {"🤍"}
      </button>
    </div>
  );
};

render(<LikeButton/>);
```
</details>

<details>
<summary>**Likes/Dislikes**</summary>

Counts for likes and dislikes using `useState` 

```jsx showLineNumbers
const LikeDislikeButton = (props: Props) => {
  const [likes, setLikes] = React.useState<number>(0);
  const [dislikes, setDislikes] = React.useState<number>(0);

  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
      <h3 style={{
        background: 'darkslateblue',
        color: 'white',
        padding: 8,
        borderRadius: 4,
        textAlign: 'center',
      }}>
        {props.label} {likes} Likes | {dislikes} Dislikes
      </h3>
      <div>
        <button
          className="button button--md button--outline button--success"
          onClick={() => setLikes(likes + 1)}
          style={{ marginRight: 8 }} // Add gap between buttons
        >
          👍
        </button>
        <button
          className="button button--md button--outline button--danger"
          onClick={() => setDislikes(dislikes + 1)}
        >
          👎
        </button>
      </div>
    </div>
  );
};

render(<LikeDislikeButton/>);
```

</details>










<!-- ### Sample Code

<BrowserWindow url="http://localhost:3000/">

<Tabs>
<TabItem value="js" label="JavaScript">

```js
function helloWorld() {
  console.log("Hello, world!");
}
```

</TabItem>
<TabItem value="py" label="Python">

```py
msg = "Hello, world!"
print(msg)
```

</TabItem>
<TabItem value="powershell" label="PowerShell">

```powershell
function Prompt { "mkeithx" + ":\" + "> " }
```

</TabItem>

<TabItem value="css" label="CSS">

```css
:root {
  --docusaurus-highlighted-code-line-bg: rgb(72, 77, 91);
}

/* If you have a different syntax highlighting theme for dark mode. */
[data-theme="dark"] {
  /* Color which works with dark mode syntax highlighting theme */
  --docusaurus-highlighted-code-line-bg: rgb(100, 100, 100);
}
```

</TabItem>
<TabItem value="git" label="Git">

```bash
git add --all && git commit -m "This is Awesome"
```

</TabItem>
</Tabs>

</BrowserWindow> -->

<!-- <p align="center">

[![techs](https://skillicons.dev/icons?i=react,js,cloudflare,github,py,ts,vscode,md,html,css&perline=20)](#)

</p> -->

## Something missing?

<Columns className='item margin-bottom-lg' > 

<Column  className='text--left item  padding--sm'>
<Card shadow='tl' style={{border: '1.5px solid var(--ifm-color-primary)'}}>
    <CardHeader>
<div class="avatar">
  <img
    class="avatar__photo avatar__photo--md"
    src="/img/logo/terminal-no-slash.svg" />
  <div class="avatar__intro">
    <div class="avatar__name"><h3>Report a Bug</h3></div>
    <!-- <small class="avatar__subtitle">Frenchy Front End Engineer</small> -->
  </div>
</div>
    </CardHeader>
    <CardBody>
    If you find issues or inconsistencies with the documentation or have
    suggestions on how to improve the project, please [file an
    issue](/community/contributing#issues).
    </CardBody>
  </Card>
  </Column>
   <Column  className='text--left item  padding--sm'>
<Card shadow='tl' style={{border: '1.5px solid var(--ifm-color-primary)'}}>
    <CardHeader>
<div class="avatar">
  <img
    class="avatar__photo avatar__photo--md"
    src="/img/logo/terminal-no-slash.svg" />
  <div class="avatar__intro">
    <div class="avatar__name"><h3>Request a Feature</h3></div>
    <!-- <small class="avatar__subtitle">Frenchy Front End Engineer</small> -->
  </div>
</div>
    </CardHeader>
    <CardBody>
For new feature requests, you can create a post through the [feature requests board](/feature-requests). Your ideas are welcome for this project!
    </CardBody>
  </Card>
  </Column>
</Columns>

<br></br>

<!-- <ReportsCardRow /> -->

<!-- If you find issues or inconsistencies with the documentation or have suggestions on how to improve the project in general, please [file an issue](/community/contributing#issues).


For new feature requests, you can create a post on the [feature requests board](/feature-requests) (Canny). Your ideas are welcome! -->

<!-- , which is a handy tool for road-mapping and allows for sorting by upvotes, which gives the core team a better indicator of what features are in high demand, as compared to GitHub issues which are harder to triage. -->

<!-- :::tip
we aim to make learning and development accessible for everyone
::: -->

:::caution[Important]
Please note that the site is constantly updating, and all content is a work in progress. Some features may not function as intended if used incorrectly. Its highly encouraged to proceed with caution and use the resources at your own discretion.
:::

<!-- if you're geeky enough to notice that the site is constantly updating—whether with new content, minor fixes, or both—it's because the website also serves as a resource for the maintainers and contributors at [**Kitiplex**](https://github.com/kitiplex). -->