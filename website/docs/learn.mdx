---
description: Learn more
slug: /learn
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import Video from "/src/components/Video";
import IframeWindow from "@site/src/components/BrowserWindow/IframeWindow";
import BrowserWindow from "@site/src/components/BrowserWindow";

# Developers

:::caution[CONSTRUCTION AREA]

This section is current under development in reference to [PR#353](https://github.com/mkeithX/mkeithx.github.io/pull/353).

:::

Learn Python, Git, and more with simple guides, fun projects, and helpful tips.

<p align="left">
  <img src="https://skillicons.dev/icons?i=python,git,javascript,react,typescript,vscode" />
</p>



<details>
<summary>Get started</summary>

<Tabs>
  <TabItem value="python" label="Python" default>

```python
msg = ("Hello, world!")
print(msg)
```

  </TabItem>
  <TabItem value="javascript" label="Javascript">

```js
function helloWorld() {
  console.log("Hello, world!");
}
```

  </TabItem>
  <TabItem value="banana" label="Banana">

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hello, World!</title>
  </head>
  <body>
    <h1>Hello, World!</h1>
  </body>
</html>
```

  </TabItem>
</Tabs>

```python
def add(x, y):
    return x + y

def subtract(x, y):
    return x - y

def multiply(x, y):
    return x * y

def divide(x, y):
    if y == 0:
        return "Error! Division by zero."
    return x / y
# highlight-start
print("Select operation:")
print("1. Add")
print("2. Subtract")
print("3. Multiply")
print("4. Divide")
# highlight-end
choice = input("Enter choice(1/2/3/4): ")


num1 = float(input("Enter first number: "))
num2 = float(input("Enter second number: "))


# highlight-next-line
if choice == '1':
    print(num1, "+", num2, "=", add(num1, num2))
elif choice == '2':
    print(num1, "-", num2, "=", subtract(num1, num2))
elif choice == '3':
    print(num1, "*", num2, "=", multiply(num1, num2))
elif choice == '4':
    print(num1, "/", num2, "=", divide(num1, num2))
else:
    print("Invalid input")
```

</details>



## Samples



Test your React using the Interactive Code Editor:

<BrowserWindow>

```jsx live noInline
const project = "The SpaceHub Project";

const Greeting = () => (
  <p>
    Introducing <br></br> {project}
  </p>
);

render(<Greeting />);
```

</BrowserWindow>

<details>
  <summary>**Real-Time Clock**</summary>

Displays the current time and updates every second. It uses `useState` to manage the time state and `useEffect` to set up and clean up a timer for real-time updates.

<BrowserWindow >

```jsx live noInline
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
render(Clock);
```
</BrowserWindow>

</details>

<details>
<summary>**Enhanced Clock**</summary>

Showing both 24-hour and 12-hour formats.

<BrowserWindow >

```jsx live noInline
function Clock(props) {
  const [date, setDate] = useState(new Date());

  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return () => clearInterval(timerID); // Cleanup timer on unmount
  }, []); // Add empty dependency array for one-time setup

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h4>
        Date: {date.toLocaleDateString()},{" "}
        {date.toLocaleDateString(undefined, { weekday: "long" })}
      </h4>
      <h4>
        Time: {date.toLocaleTimeString([], { hour12: false })} ·{" "}
        {date.toLocaleTimeString([])}
      </h4>
    </div>
  );
}

render(<Clock />);
```
</BrowserWindow>


</details>

<details>
<summary>**Likes Counter**</summary>

A simple component that allows users to increase a like count.

```jsx
const LikeButton = (props: Props) => {
  const [likes, increaseLikes] = React.useState < number > 0;

  return (
    <div
      style={{ display: "flex", flexDirection: "column", alignItems: "center" }}
    >
      <h3
        style={{
          background: "darkslateblue",
          color: "white",
          padding: 8,
          borderRadius: 4,
          textAlign: "center", // Center text within the h3
        }}
      >
        {props.label} {likes} Likes
      </h3>
      <button
        className="button button--md button--danger"
        onClick={() => increaseLikes((c) => c + 1)}
      >
        {"🤍"}
      </button>
    </div>
  );
};

render(<LikeButton />);
```

</details>

<details>
<summary>**Likes/Dislikes**</summary>

Counts for likes and dislikes using `useState`

```jsx
const LikeDislikeButton = (props: Props) => {
  const [likes, setLikes] = React.useState < number > 0;
  const [dislikes, setDislikes] = React.useState < number > 0;

  return (
    <div
      style={{ display: "flex", flexDirection: "column", alignItems: "center" }}
    >
      <h3
        style={{
          background: "darkslateblue",
          color: "white",
          padding: 8,
          borderRadius: 4,
          textAlign: "center",
        }}
      >
        {props.label} {likes} Likes | {dislikes} Dislikes
      </h3>
      <div>
        <button
          className="button button--md button--outline button--success"
          onClick={() => setLikes(likes + 1)}
          style={{ marginRight: 8 }} // Add gap between buttons
        >
          👍
        </button>
        <button
          className="button button--md button--outline button--danger"
          onClick={() => setDislikes(dislikes + 1)}
        >
          👎
        </button>
      </div>
    </div>
  );
};

render(<LikeDislikeButton />);
```

</details>
